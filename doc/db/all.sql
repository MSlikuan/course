drop table if exists `tset`;

CREATE TABLE `test`  (
                               `id` int(0) NOT NULL AUTO_INCREMENT,
                               `name` varchar(255) NULL,
                               PRIMARY KEY (`id`)
);

